# 代码审查标准与流程手册

> 版本 v1.0 | 2026-06-13
>
> 核心原则：**审查的目标是提升代码质量和团队能力，而不是找茬。**

---

## 目录

1. [审查核心理念](#1-审查核心理念)
2. [审查范围与触发条件](#2-审查范围与触发条件)
3. [问题分级体系](#3-问题分级体系)
4. [审查检查清单](#4-审查检查清单)
5. [审查流程](#5-审查流程)
6. [角色与职责](#6-角色与职责)
7. [评论撰写规范](#7-评论撰写规范)
8. [常见反模式速查](#8-常见反模式速查)
9. [自动化工具配置建议](#9-自动化工具配置建议)
10. [PR 模板](#10-pr-模板)
11. [度量与持续改进](#11-度量与持续改进)
12. [快速参考卡片](#12-快速参考卡片)

---

## 1. 审查核心理念

### 审查关注什么

| 维度 | 说明 |
|------|------|
| **正确性** | 代码是否实现了预期功能？边界情况是否处理？ |
| **安全性** | 是否有注入、XSS、权限绕过等安全漏洞？ |
| **可维护性** | 6 个月后别人（或你）还能看懂的代码？ |
| **性能** | 是否有明显的 N+1 查询、不必要的内存分配？ |
| **可测试性** | 关键路径是否有测试覆盖？代码是否易于测试？ |

### 审查不关注什么

- **代码风格** — 交给 ESLint / Prettier / Black 等自动化工具
- **个人偏好** — "我不喜欢这种写法" 不构成有效 Review 意见
- **架构大改** — 除非有明确的正确性问题，否则不应在 Review 阶段推翻已有设计

### 核心原则

1. **每一条评论都应该教人点什么** — 不只是说"这里不对"，要说"为什么不对"和"更好的做法是什么"
2. **建议而非命令** — 用 "考虑使用 X，因为 Y" 替代 "改成 X"
3. **一次审查，完整反馈** — 不要分批提意见，一次性给完
4. **好的代码也要表扬** — 看见巧妙的解法和干净的代码，说出来

---

## 2. 审查范围与触发条件

### 必须审查的变更

- 所有合并到 `main` / `master` 分支的 Pull Request
- 涉及核心业务逻辑的变更
- 涉及认证、授权、支付、数据的变更
- 新增或修改公共 API（包括内部服务接口）
- 数据库 Schema 变更

### 可以不审查的变更

- 纯文档修正（typo 修复）
- 仅配置文件变更且已在 CI 中验证
- 紧急热修复（事后补审）
- 自动生成的代码（需在 PR 中标注）

### PR 规模建议

| PR 大小 | 建议 |
|---------|------|
| < 200 行 | 理想，审查效率最高 |
| 200 - 500 行 | 合理，一次性审完 |
| 500 - 1000 行 | 偏大，建议拆分成多个 PR |
| > 1000 行 | 应拆分，审查质量会明显下降 |

---

## 3. 问题分级体系

使用统一的分级标记，让问题和期望一目了然：

### 🔴 阻断项（Blocker）— 必须修复后才能合并

| 类别 | 典型场景 |
|------|---------|
| 安全漏洞 | SQL/NoSQL 注入、XSS、CSRF、权限绕过、敏感信息泄露 |
| 数据风险 | 数据丢失或损坏、错误的删除逻辑、缺少事务保护 |
| 并发问题 | 竞态条件、死锁、非原子操作 |
| 契约破坏 | 改动公共 API 的入参/返回值格式、破坏性数据库迁移 |
| 关键路径错误处理缺失 | 支付流程、认证流程中的异常未被捕获 |
| 测试失败 | CI 中的测试未通过 |

### 🟡 建议（Suggestion）— 应该修复，但可协商

| 类别 | 典型场景 |
|------|---------|
| 输入验证 | 缺少参数校验、边界值未处理 |
| 可读性 | 命名不清、逻辑晦涩、魔法数字 |
| 测试覆盖 | 关键行为缺少测试 |
| 性能 | N+1 查询、不必要的循环嵌套、大量不必要的对象创建 |
| 代码重复 | 可以提取为公共函数的重复逻辑 |
| 错误处理 | 非关键路径的错误被吞掉 |

### 💭 小建议（Nit）— 锦上添花，不改也 OK

| 类别 | 典型场景 |
|------|---------|
| 命名微调 | 变量名可以更精确但当前也能理解 |
| 文档补充 | 可以加一段注释解释为什么这样设计 |
| 替代方案 | "还有另一种实现方式值得考虑" |
| 代码组织 | 函数的排列顺序可以更合理 |

---

## 4. 审查检查清单

### 4.1 通用检查项（每次审查必过）

```
□ 代码是否正确实现了需求描述的功能？
□ 是否有测试覆盖新增/修改的逻辑？
□ 测试是否覆盖了边界情况和错误路径？
□ 变量和函数命名是否清晰、符合项目约定？
□ 是否有被注释掉的代码残留？
□ 是否有调试用的 console.log / print 残留？
□ 新增的依赖是否必要？版本是否合理？
□ 是否有硬编码的配置值（应提取到配置文件或环境变量）？
```

### 4.2 安全专项检查

```
□ 用户输入是否经过了验证和清理？
□ SQL/数据库查询是否使用了参数化查询？
□ 用户提供的内容渲染到页面时是否转义（防 XSS）？
□ 敏感操作（删除、支付、权限变更）是否有二次确认和日志？
□ 认证和授权是否在每个端点正确执行？
□ 密钥、Token、密码是否硬编码在代码中？
□ API 返回的错误信息是否泄露了内部实现细节？
```

### 4.3 后端专项检查

```
□ 数据库查询是否有适当的索引？
□ 是否避免了 N+1 查询？
□ 事务边界是否正确？
□ 外部服务调用是否有超时和降级策略？
□ 日志级别是否合适？关键路径是否有 trace 级别的日志？
□ API 响应格式是否符合项目约定？
□ 是否有合适的 HTTP 状态码？
□ 分页接口是否有合理的 page_size 上限？
```

### 4.4 前端专项检查

```
□ 组件是否有合理的 key（列表渲染）？
□ 异步操作是否处理了 loading / error / empty 三种状态？
□ useEffect 是否有正确的依赖数组和清理函数？
□ 是否避免了不必要的重新渲染？
□ 表单是否有验证和错误提示？
□ 国际化文案是否提取正确？
□ 是否考虑了键盘导航和无障碍访问？
□ 移动端响应式是否正常？
```

---

## 5. 审查流程

### 5.1 标准流程

```
作者                        审查者
 │                            │
 ├─ 创建 PR                   │
 ├─ 填写 PR 模板              │
 ├─ 自审（自查清单）           │
 ├─ 标记为 Ready for Review ──→│
 │                            ├─ 收到通知
 │                            ├─ 阅读 PR 描述
 │                            ├─ 拉取代码，本地运行（视复杂程度）
 │                            ├─ 逐文件审查
 │                            ├─ 按优先级标记评论
 │                            ├─ 提交审查意见（Approve / Request Changes）
 │←── 收到审查反馈 ────────────┤
 ├─ 逐条处理反馈               │
 │   ├─ 赞同 → 修改后标记 resolved
 │   ├─ 不赞同 → 回复讨论原因
 │   └─ 不明确 → 当面沟通
 ├─ 推送修改                   │
 │←── 重新审查（如有必要）─────┤
 ├─ 所有评论 resolved          │
 ├─ CI 通过                    │
 ├─ 合并                       │
```

### 5.2 审查时限

| 状态 | 建议时限 |
|------|---------|
| 审查者响应 | PR 提交后 **4 工作小时内** 开始审查 |
| 审查完成 | PR < 500 行：**1 工作日内**；PR > 500 行：**2 工作日内** |
| 作者响应 | 收到反馈后 **1 工作日内** 回复或修改 |
| 紧急 PR | 沟通确认后，**2 小时内** 完成审查 |

### 5.3 冲突处理

当作者与审查者意见不一致时：
1. 先在 PR 评论中讨论，双方陈述理由
2. 若无法达成一致，寻求第三人（Tech Lead / 架构师）仲裁
3. 仲裁结果作为最终决策，不纠结

---

## 6. 角色与职责

### 作者（PR 提交者）

- **提交前自审**：用检查清单自查一遍
- **小 PR**：尽量控制在 500 行以内
- **清晰的描述**：说明做了什么、为什么这样做、有什么风险
- **积极响应**：及时处理审查反馈
- **合并前确认**：确保 CI 通过、所有评论已解决

### 审查者

- **关注重点**：正确性 > 安全性 > 可维护性 > 性能 > 风格
- **及时响应**：在规定时限内完成审查
- **建设性反馈**：每条评论都说明 why 和 how
- **区分优先级**：用 🔴🟡💭 标记，不让 Nit 淹没 Blocker
- **知识传递**：审查也是教学机会

### Tech Lead / 团队负责人

- 确保审查流程被执行
- 仲裁争议
- 定期回顾审查质量和效率
- 维护和更新审查标准

---

## 7. 评论撰写规范

### 7.1 评论模板

```
[优先级标记] **[类别]: [简短标题]**
[具体位置]：[具体问题描述]

**Why：** [为什么这是个问题，潜在的风险或影响]

**Suggestion：**
- [具体的修改建议，最好附代码示例]

**Reference：**（可选，链接到最佳实践文档或相关资源）
```

### 7.2 优秀评论示例

```
🔴 **Security: SQL Injection Risk**
`src/services/user.ts:42` — 用户输入的 `name` 参数直接拼接到 SQL 查询中。

**Why：** 攻击者可以通过 name 参数注入恶意 SQL，例如:
`'; DROP TABLE users; --`，导致数据丢失。

**Suggestion：**
使用参数化查询替代字符串拼接：
```ts
const user = await db.query(
  'SELECT * FROM users WHERE name = $1',
  [name]
);
```

**Reference：** OWASP SQL Injection Prevention Cheat Sheet
```

```
🟡 **Performance: N+1 Query in Loop**
`src/services/order.ts:78-85` — 在循环内逐条查询订单详情。

**Why：** 当订单数为 100 时，会触发 101 次数据库查询（1次查订单 + 100次查详情），
在数据量大时会造成严重性能问题。

**Suggestion：**
使用 JOIN 或批量查询替换：
```ts
const orderIds = orders.map(o => o.id);
const details = await db.query(
  'SELECT * FROM order_details WHERE order_id = ANY($1)',
  [orderIds]
);
const detailMap = groupBy(details, 'order_id');
return orders.map(o => ({ ...o, details: detailMap[o.id] || [] }));
```
```

```
💭 **Nit: More Descriptive Variable Name**
`src/utils/helpers.ts:15` — 变量名 `d` 不够直观。

**Suggestion：**
考虑改为 `daysSinceLastLogin` 或 `inactiveDays`，
因为从上下文看这是计算用户不活跃天数。
```

### 7.3 避免的评论方式

| ❌ 不好的评论 | ✅ 好的评论 |
|-------------|-----------|
| "这不对" | "这里在并发场景下可能出现竞态条件，因为……" |
| "改一下" | "建议在调用前加空值检查，因为……" |
| "这种写法很烂" | "这种嵌套方式可读性不太好，考虑用提前返回（early return）简化" |
| "按照我的方式写" | "方案 B 在可测试性方面有优势，因为它将副作用隔离了。你倾向哪个？" |

---

## 8. 常见反模式速查

### 代码层面

| 反模式 | 识别特征 | 改进方向 |
|--------|---------|---------|
| **上帝函数** | 单个函数超过 50 行，做太多事 | 按职责拆分为多个小函数 |
| **魔法数字** | 代码中直接出现 `7`, `30`, `500` 等 | 提取为命名常量 |
| **深层嵌套** | 超过 3 层嵌套的 if/for | Early return、提取函数 |
| **过度抽象** | 只有一个实现类的接口 | YAGNI，需要时再抽象 |
| **异常吞没** | `try { ... } catch(e) {}` | 至少记录日志，或向上抛出 |
| **布尔陷阱** | `fetchData(true, false, true)` | 改为 options 对象 |

### 流程层面

| 反模式 | 表现 | 改进方向 |
|--------|-----|---------|
| **巨型 PR** | 一次提交包含多个不相关改动 | 拆分为原子化 PR |
| **僵尸 PR** | PR 长时间无人审查 | 设定 SLA，自动化提醒 |
| **审查橡皮图章** | 所有 PR 都是 LGTM | 建立最小评论数要求 |
| **风格战争** | 审查主要在讨论代码格式 | 用自动化工具解决风格问题 |

---

## 9. 自动化工具配置建议

将能自动化的检查交给工具，让审查者专注于工具无法判断的事。

### 推荐工具链

| 层级 | 工具 | 作用 |
|------|------|------|
| **代码风格** | ESLint + Prettier (JS/TS) / Black + isort (Python) / gofmt (Go) | 自动格式化，消除风格争议 |
| **静态分析** | SonarQube / ESLint security rules / Bandit (Python) | 检测常见漏洞和代码异味 |
| **类型检查** | TypeScript (strict mode) / mypy (Python) | 在编译期捕获类型错误 |
| **测试** | Jest / pytest + coverage threshold | 确保测试覆盖率不下降 |
| **CI 门禁** | GitHub Actions / GitLab CI / Jenkins | 自动化运行以上检查 |
| **依赖检查** | Dependabot / Renovate / Snyk | 自动发现和更新有漏洞的依赖 |

### CI 流水线建议配置

```yaml
# .github/workflows/pr-check.yml (示例)
name: PR Quality Check
on: [pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run lint        # ESLint + Prettier
      - run: npm run type-check  # TypeScript strict

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm test -- --coverage
      # 覆盖率下降超过 2% 则失败
      - run: npx coverage-diff --threshold 2%

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm audit --audit-level=high  # 高危漏洞阻断
```

---

## 10. PR 模板

### 使用方式

将以下内容保存为 `.github/PULL_REQUEST_TEMPLATE.md` 或在 Git 平台配置 PR 模板。

```markdown
## 📝 变更概述
<!-- 简要描述这个 PR 做了什么 -->

## 🎯 关联 Issue
Closes #

## 📋 变更类型
- [ ] 🐛 Bug 修复
- [ ] ✨ 新功能
- [ ] 🔧 重构（不改变功能）
- [ ] 📚 文档更新
- [ ] 🚀 性能优化
- [ ] 🔒 安全修复

## 🧪 测试
<!-- 描述你做了什么测试 -->
- [ ] 单元测试已添加/更新
- [ ] 集成测试已添加/更新
- [ ] 手动测试已完成

## 📸 截图（前端变更）
<!-- 如有 UI 变更请附上截图 -->

## ⚠️ 风险点
<!-- 这个变更有什么潜在风险？有无破坏性变更？ -->
- 

## 👀 审查指引
<!-- 引导审查者关注的重点区域 -->
- 重点关注：
- 可以跳过：

## ✅ 自审清单
- [ ] 代码符合项目编码规范
- [ ] 没有调试代码或注释残留
- [ ] 命名清晰，无魔法数字
- [ ] 错误处理完善
- [ ] API 变更已在文档中更新
- [ ] PR 控制在合理大小（< 500 行）
```

---

## 11. 度量与持续改进

### 建议跟踪的指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| PR 到首次审查的时长 | < 4 小时 | 衡量审查响应速度 |
| PR 从创建到合并的时长 | < 1 天（小 PR） | 衡量整体效率 |
| 审查发现缺陷数 / PR | > 0 | 有实质性反馈说明审查有效 |
| 合并后发现的缺陷数 | 趋近于 0 | 衡量审查的漏检率 |
| PR 平均大小 | < 300 行 | 小 PR 审查质量更高 |
| Nit 评论占比 | < 30% | Nit 太多说明审查流于表面 |

### 定期回顾（建议每月一次）

每次回顾时讨论：
1. 上个月有没有合并后才发现的问题？根本原因是什么？
2. 审查中最常见的哪类问题？是否可以通过自动化工具解决？
3. 有没有好的审查案例可以分享学习？
4. 流程有没有可以优化的地方？

---

## 12. 快速参考卡片

> 打印出来贴工位上，审查时对照使用。

```
┌─────────────────────────────────────────────────────────────┐
│                    代码审查快速参考                            │
├─────────────────────────────────────────────────────────────┤
│  🔴 阻断：安全问题、数据风险、并发Bug、API破坏、CI挂掉       │
│  🟡 建议：输入验证、可读性、测试、性能、重复代码、异常处理     │
│  💭 建议(轻)：命名微调、注释补充、替代思路                      │
├─────────────────────────────────────────────────────────────┤
│  审查必查：                                                  │
│  □ 逻辑正确性    □ 安全漏洞    □ 错误处理    □ 测试覆盖       │
│  □ 命名清晰度    □ 无调试残留  □ 边界情况    □ 无过度工程     │
├─────────────────────────────────────────────────────────────┤
│  评论格式：[标记] **类别: 标题**                               │
│  位置: xxx  |  Why: xxx  |  Suggestion: xxx                  │
├─────────────────────────────────────────────────────────────┤
│  PR 规模：< 200行 ✨理想 | 200-500 👍合理 | >500 ⚠️ 考虑拆分   │
│  审查时限：< 500行 → 1天内 | > 500行 → 2天内                  │
│  作者响应：收到反馈后 1天内                                    │
├─────────────────────────────────────────────────────────────┤
│  核心口诀：审查教人，不是找茬；建议做法，不是命令               │
└─────────────────────────────────────────────────────────────┘
```

---

## 附录：落地建议

### 第 1 周：打好基础
- [ ] 团队会议宣讲审查标准（30 分钟）
- [ ] 配置 CI 门禁（Lint + Type Check + Test）
- [ ] 设置 PR 模板
- [ ] 选定首批试行项目

### 第 2-4 周：试运行
- [ ] 所有 PR 必须至少 1 人审查通过
- [ ] 使用分级标记（🔴🟡💭）
- [ ] 每周 5 分钟回顾审查效果

### 第 2 个月起：固化
- [ ] 将审查指标纳入团队 Dashboard
- [ ] 每月回顾和优化流程
- [ ] 优秀审查案例分享（每月 1 次，15 分钟）
- [ ] 根据团队反馈微调标准

---

> **记住：好的代码审查文化不是一蹴而就的。它需要刻意练习、持续反馈、以及团队每个人的投入。**
>
> 这份手册是一个起点——用它来对齐认知，然后在实践中不断演进。
