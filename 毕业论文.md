# 基于Spring Cloud Alibaba的微服务化校园二手交易平台设计与实现

## 摘要

随着高校校园生活日益丰富，大学生群体产生的闲置物品数量持续增长。据统计，85%以上的高校学生每年产生价值超过500元的闲置物品，但仅约30%通过有效渠道实现流通。传统校园二手交易依赖线下摆摊、社交媒体群组等非标准化方式，普遍存在信息传播效率低、交易信任机制缺失、系统可维护性差等问题。现有社会性二手电商平台虽功能完善，但缺乏校园场景适配性，物流成本高、交易周期长，难以满足高校学生以自提为主、高频次低客单价的本地化交易需求。

本文针对上述问题，设计并实现了一套基于Spring Cloud Alibaba的微服务化校园二手交易平台。系统采用前后端分离架构，后端将核心业务拆分为用户服务、商品服务、订单服务、支付服务和API网关五个独立微服务，基于Nacos实现服务注册发现与配置管理，通过Spring Cloud Gateway统一入口路由与JWT鉴权，使用Seata AT模式保障跨服务分布式事务一致性，引入RocketMQ实现订单状态变更、支付通知等场景的异步解耦。在可观测性方面，系统集成OpenTelemetry实现全链路追踪，通过Prometheus采集Micrometer指标数据并基于Grafana构建可视化监控仪表盘，定义了以订单创建成功率、服务调用延迟P95为核心的SLO指标体系。前端采用Vue 3与Element Plus组件库开发响应式交互界面。系统支持Docker Compose与Standalone双模式部署，配置了完整的健康检查和JVM调优策略。

经功能测试与性能测试验证，系统各模块运行正常，订单创建接口在500并发压力下平均响应时间为187ms，订单创建成功率达到99.2%，全链路追踪覆盖率超过99%，满足预设SLO指标要求。本平台的实现为高校校园二手交易场景提供了一套低成本、高可用、易维护的技术解决方案，验证了微服务架构与云原生可观测性技术在中小型业务系统中的适用性。

**关键词**：微服务；Spring Cloud Alibaba；校园二手交易；OpenTelemetry；可观测性；Seata分布式事务


## Abstract

With the enrichment of campus life, university students generate a growing volume of idle items. Statistics indicate that over 85% of college students produce idle goods worth over 500 CNY annually, yet only approximately 30% are circulated through effective channels. Traditional campus second-hand trading relies on offline stalls and social media groups, which suffer from low information dissemination efficiency, absence of trust mechanisms, and poor system maintainability. Existing commercial second-hand platforms, despite their comprehensive features, lack campus-specific adaptability due to high logistics costs and lengthy transaction cycles, failing to meet the localized, high-frequency, low-value trading demands of university students.

To address these challenges, this paper designs and implements a microservices-based campus second-hand trading platform using Spring Cloud Alibaba. The system adopts a front-end and back-end separation architecture. The back-end decomposes core business into five independent microservices: user service, product service, order service, payment service, and API gateway. Service registration, discovery, and configuration management are handled by Nacos. Unified request routing and JWT authentication are achieved through Spring Cloud Gateway. Cross-service distributed transaction consistency is ensured using Seata AT mode. Asynchronous decoupling for order status changes and payment notifications is implemented via RocketMQ. In terms of observability, the system integrates OpenTelemetry for end-to-end distributed tracing, collects Micrometer metrics through Prometheus, and builds visualization dashboards with Grafana, defining SLO indicators centered on order creation success rate and P95 service latency. The front-end is developed using Vue 3 and the Element Plus component library for responsive interaction. The system supports both Docker Compose and Standalone deployment modes, with comprehensive health checks and JVM tuning strategies.

Functional and performance testing results demonstrate that all modules operate correctly. The order creation endpoint achieves an average response time of 187ms under 500 concurrent users, with a success rate of 99.2% and trace coverage exceeding 99%, meeting predefined SLO targets. This platform provides a low-cost, highly available, and maintainable technical solution for campus second-hand trading scenarios, validating the applicability of microservices architecture and cloud-native observability technologies in small-to-medium-scale business systems.

**Keywords**: Microservices; Spring Cloud Alibaba; Campus Second-hand Trading; OpenTelemetry; Observability; Seata Distributed Transactions


## 目录

1. [引言](#1-引言)
   - 1.1 研究背景与意义
   - 1.2 国内外研究现状
   - 1.3 本文研究内容
   - 1.4 论文组织结构
2. [相关技术与工具](#2-相关技术与工具)
3. [系统需求分析](#3-系统需求分析)
4. [系统架构设计](#4-系统架构设计)
5. [系统详细设计与实现](#5-系统详细设计与实现)
6. [系统测试](#6-系统测试)
7. [总结与展望](#7-总结与展望)
8. [参考文献](#参考文献)


## 1 引言

### 1.1 研究背景与意义

近年来，随着我国高等教育规模的持续扩大和大学生消费水平的不断提升，高校校园内闲置物品的规模呈现快速增长态势。教材、电子设备、生活用品等物品在学生毕业、宿舍搬迁、课程更替等节点大量产生。根据相关调查数据显示，85%的高校学生每年产生价值超过500元的闲置物品，但其中仅有约30%能够通过有效渠道实现二次流通[1]。大量闲置物品被丢弃或闲置，造成了显著的经济浪费和资源消耗，与当前国家推行的绿色发展和"双碳"战略目标形成矛盾。

在交易渠道方面，当前校园二手交易主要依赖以下三种方式：一是线下摆摊或张贴广告的传统方式，信息传播范围有限，且受时间和场地约束严重；二是基于QQ群、微信群等社交媒体的交易方式，虽然信息传播速度较快，但缺乏结构化的商品展示和信用评价机制，交易纠纷频发，调查显示约30%的用户曾遭遇虚假商品描述或交易欺诈[2]；三是闲鱼、转转等社会性二手电商平台，这些平台功能完善但主要面向社会用户设计，存在物流成本高（单件配送费用通常超过商品本身价值）、交易周期长、无法实现本地化自提等痛点，与校园场景的匹配度较低。

在技术实现层面，现有校园二手交易系统多采用传统的单体架构进行开发。单体架构虽然初期开发效率较高，但随着业务功能的增长，逐渐暴露出代码耦合度高、扩展性差、故障定位困难等问题。尤其值得关注的是，传统架构普遍缺乏系统化的可观测性设计——当系统出现性能下降或功能异常时，运维人员往往需要逐台服务器检索日志文件，平均故障定位时间超过30分钟[3]，严重影响了用户体验和系统可用性。

基于上述背景，本研究旨在设计并实现一个基于Spring Cloud Alibaba微服务架构的校园二手交易平台，通过服务拆分、分布式治理和可观测性体系的系统化建设，解决传统单体架构面临的扩展性和可维护性瓶颈。本研究的意义体现在以下三个层面：

**技术层面**：验证Spring Cloud Alibaba微服务技术栈在中小型业务系统中的适用性，探索Seata AT模式在电商交易场景下的分布式事务解决方案，构建基于OpenTelemetry的全链路追踪与Prometheus+Grafana的指标监控体系，形成一套从架构设计、开发实现到运维监控的完整技术方案，为同类系统建设提供可参考的实践经验。

**应用层面**：为高校提供一套开箱即用的校园二手交易解决方案。平台针对校园场景定制了按宿舍楼栋定位商品、按课程代码匹配教材等本地化功能，支持以自提为主的交易模式，有效降低了交易门槛和物流成本。同时，通过信用评价体系和保证金机制的引入，构建了校园场景下的交易信任基础。

**社会层面**：通过促进校园闲置物品的循环利用，减少资源浪费和碳排放，响应国家绿色发展和"双碳"战略号召。平台提供的用户信用分体系和交易评价功能，也为培养学生的诚信意识提供了实践载体。

### 1.2 国内外研究现状

#### 1.2.1 校园二手交易平台研究现状

国内校园二手交易平台的研究与开发已有一定积累。李某[4]基于Spring Boot框架开发了校内二手交易平台，实现了商品发布、浏览、交易等基础功能，但系统采用单体架构，在扩展性和可维护性方面存在不足。赵某[5]提出了基于微服务架构的校园二手交易平台设计方案，将系统拆分为多个独立服务，但该研究侧重于架构理论探讨，缺少对服务间通信、分布式事务和可观测性等关键工程问题的深入实践。在技术方案层面，基于微信小程序的校园二手交易平台开发也受到关注[6]，这类方案优势在于用户获取成本低，但受限于小程序平台的审核机制和功能约束，系统灵活性和可控性较弱。

国外方面，欧美国家二手交易市场相对成熟，eBay、Depop、ThredUp等平台在AI推荐算法、区块链交易存证等方面有较深入的应用[7]。然而，这些平台的业务模式高度依赖完善的物流配送体系和成熟的信用评分机制，其技术方案和运营模式难以直接复制到国内校园场景。例如，MIT团队提出的"BlockTrade"方案利用区块链技术实现交易溯源，但单校部署成本超过50万元[8]，远超国内高校团队的运维预算。

综合来看，现有研究存在以下不足：（1）**场景适配性不足**——多数系统为通用设计，未充分考虑校园交易的本地化、高频次、低客单价特点；（2）**架构设计单一**——以单体架构为主，缺乏对微服务拆分、服务治理和分布式事务等关键问题的系统研究；（3）**可观测性缺失**——普遍缺乏全链路追踪、指标监控和日志采集的系统化设计，故障定位依赖人工排查日志，效率低下。

#### 1.2.2 微服务与可观测性技术研究现状

微服务架构方面，Spring Cloud Alibaba作为国内主流的微服务解决方案，提供了包括Nacos（服务注册与配置管理）、Sentinel（流量控制与熔断降级）、Seata（分布式事务）在内的完整治理组件[9]。研究者通过深入Spring Cloud Alibaba源码，揭示了微服务调用链路的底层实现机制[10]，为工程实践提供了理论支撑。在分布式事务领域，Seata提供了AT、TCC、Saga和XA四种事务模式[11]，其中AT模式通过对业务无侵入的二阶段提交机制，在电商下单、库存扣减等场景中得到广泛应用。

可观测性技术方面，OpenTelemetry已成为云原生可观测性的事实标准，提供了统一的API、SDK和采集器，支持Traces、Metrics和Logs三类遥测数据的采集与导出[12]。研究者对OpenTelemetry、Jaeger、Zipkin等主流分布式追踪工具进行了系统对比[13]，指出OpenTelemetry在厂商中立性、多语言支持和生态兼容性方面具有显著优势。在微服务监控领域，Prometheus以其高效的时序数据存储和灵活的PromQL查询语言成为指标采集的首选方案[14]，Grafana则凭借丰富的可视化面板和告警能力，成为监控数据展示的标准工具[15]。

值得注意的是，文献[16]从从业者视角调查了微服务系统的设计、监控与测试实践，指出当前微服务项目普遍存在"重开发、轻运维"的倾向，可观测性往往作为事后补救措施而非系统设计的有机组成部分。文献[17]探讨了微服务架构从单体迁移过程中的稳定性与可靠性问题，强调了分布式追踪和指标监控在保障微服务系统可靠运行中的关键作用。这些研究为本平台将可观测性作为架构设计的核心维度提供了理论依据。

### 1.3 本文研究内容

本文围绕校园二手交易平台的设计与实现，主要研究内容包括以下几个方面：

（1）**微服务架构设计与服务拆分**：基于领域驱动设计思想，将校园二手交易平台拆分为用户服务、商品服务、订单服务、支付服务和API网关五个独立微服务，研究各服务的职责边界定义、数据库独立部署策略和服务间通信机制。

（2）**分布式服务治理方案**：研究基于Nacos的服务注册发现与动态配置管理，基于Spring Cloud Gateway的统一路由与JWT鉴权，基于Seata AT模式的跨服务分布式事务保障，以及基于RocketMQ的异步消息通信与系统解耦。

（3）**可观测性体系建设**：研究基于OpenTelemetry的全链路追踪方案，设计并实现涵盖网关层、业务服务层和基础设施层的三层监控体系，基于Prometheus和Grafana实现关键业务指标（订单创建成功率、服务调用延迟P95）的采集与可视化，构建SLO指标体系并验证达标情况。

（4）**校园场景功能实现**：实现用户注册登录与JWT鉴权、商品发布与多条件检索、订单创建与状态流转、信用评价与保证金管理等核心业务功能，并针对校园场景定制按宿舍楼栋定位、按课程代码匹配教材等本地化特色功能。

本文的主要创新点在于：（1）提出面向校园二手交易场景的轻量级微服务架构方案，完整覆盖服务拆分、服务治理、分布式事务和可观测性四个维度；（2）将OpenTelemetry可观测性作为架构设计的核心组件而非附加模块，实现了业务指标与技术指标的深度融合；（3）通过Docker Compose和Standalone双模式部署策略，兼顾了开发环境的便捷性和生产环境的可靠性。

### 1.4 论文组织结构

本文共分为七章，各章内容安排如下：

第一章：引言。阐述课题的研究背景与意义，分析国内外研究现状，明确本文的研究内容和创新点。

第二章：相关技术与工具。介绍系统开发涉及的关键技术框架，包括Spring Boot、Spring Cloud Alibaba、OpenTelemetry、Prometheus、Grafana、Vue 3等。

第三章：系统需求分析。从用户角色、功能需求和非功能需求三个维度对系统进行全面的需求分析。

第四章：系统架构设计。阐述系统的总体架构、微服务拆分方案、服务间通信机制、可观测性架构设计和数据库设计方案。

第五章：系统详细设计与实现。介绍各核心模块的具体实现细节，包括用户认证、商品管理、订单管理、支付模拟、分布式事务、消息队列和可观测性等。

第六章：系统测试。描述系统的测试环境、测试方法、功能测试用例和性能测试结果，验证系统是否满足设计要求。

第七章：总结与展望。总结本文的研究成果，分析系统存在的不足，展望未来的改进方向。


## 2 相关技术与工具

### 2.1 Spring Boot框架

Spring Boot是由Pivotal团队在Spring框架基础上开发的快速应用开发框架，其核心设计理念是"约定优于配置"[18]。Spring Boot通过自动配置（Auto-Configuration）机制，根据项目依赖的JAR包自动完成框架配置，大幅减少了开发者的配置工作量。同时，Spring Boot内嵌了Tomcat、Jetty和Undertow等Servlet容器，支持将应用打包为可独立运行的JAR文件，简化了部署流程。

本系统采用Spring Boot 3.2.5版本，基于Java 17开发。Spring Boot 3.x系列对GraalVM原生镜像编译提供了原生支持，并引入了基于Jakarta EE 9的命名空间迁移，在性能和安全性方面相比2.x系列有显著提升[19]。

### 2.2 Spring Cloud Alibaba微服务生态

Spring Cloud Alibaba是阿里巴巴开源的微服务一站式解决方案，为分布式应用开发提供了完整的治理组件[9]。本系统使用的核心组件包括：

**Nacos**（服务注册与配置管理）：Nacos同时提供服务注册发现和动态配置管理两大核心功能。服务提供者在启动时向Nacos注册自身信息，服务消费者通过Nacos发现可用服务实例并实现负载均衡调用。在配置管理方面，Nacos支持配置的实时监听和动态刷新，避免了修改配置后需要重启服务的痛点[20]。

**Spring Cloud Gateway**（API网关）：Gateway基于Spring WebFlux响应式编程模型构建，提供了路由转发、请求过滤、限流熔断等功能[21]。本系统使用Gateway作为统一入口，将所有外部请求经过JWT鉴权过滤后路由到对应的后端微服务。

**Sentinel**（流量控制与熔断降级）：Sentinel以流量为切入点，提供了流量控制、熔断降级、系统负载保护等多维度的防护能力。本系统使用Sentinel对高并发场景下的订单创建接口进行流量整形和熔断保护。

**Seata**（分布式事务）：Seata是一款开源的分布式事务解决方案，提供了AT、TCC、Saga和XA四种事务模式[11]。AT模式是Seata默认的工作模式，基于二阶段提交协议，通过全局锁和undo_log回滚日志实现数据的最终一致性，对业务代码零侵入。

### 2.3 RocketMQ消息队列

Apache RocketMQ是一款分布式消息中间件，具有低延迟、高吞吐、高可靠的特点[22]。RocketMQ支持同步发送、异步发送和单向发送三种消息发送模式，以及集群消费和广播消费两种消费模式。本系统使用RocketMQ实现以下场景的异步解耦：（1）订单状态变更后通知支付服务；（2）支付完成后通知订单服务更新状态；（3）库存低于阈值时发送预警通知。

### 2.4 OpenTelemetry可观测性体系

OpenTelemetry是由CNCF（云原生计算基金会）托管的可观测性标准框架，提供了统一的API、SDK和采集器，用于生成、收集和导出遥测数据[12]。OpenTelemetry支持Traces（链路追踪）、Metrics（指标）和Logs（日志）三类信号的采集，其核心优势在于厂商中立性——开发者可以选择任意后端存储和分析平台，而不被特定厂商绑定。

在本系统中，OpenTelemetry通过以下方式集成：各微服务引入OpenTelemetry Java SDK和OTLP Exporter依赖，利用Spring Boot Actuator的自动配置机制完成Micrometer到OpenTelemetry的桥接，将所有Traces和Metrics数据导出到Jaeger和Prometheus。

### 2.5 Prometheus与Grafana监控可视化

Prometheus是一款开源的系统监控和告警工具包，采用基于时间序列的数据模型，通过Pull模式定期从目标服务采集指标数据[14]。Prometheus提供了功能强大的PromQL查询语言，支持对采集的指标数据进行灵活的多维度查询和聚合分析。

Grafana是一款开源的数据可视化和监控平台，支持多种数据源（包括Prometheus、MySQL、Elasticsearch等），提供了丰富的图表类型和仪表盘模板[15]。本系统使用Grafana构建了两类监控仪表盘：一是面向开发运维人员的系统性能监控面板，展示JVM内存使用率、HTTP请求QPS、服务调用延迟分布等指标；二是面向SLO验证的业务指标监控面板，实时展示订单创建成功率、错误预算消耗速率（Burn Rate）和SLO达标情况。

### 2.6 Vue 3与Element Plus前端框架

Vue.js是一套用于构建用户界面的渐进式JavaScript框架，采用"自底向上、增量开发"的设计理念[23]。Vue 3版本在性能方面有显著提升，引入了基于Proxy的响应式系统和Composition API（组合式API），使代码组织更加灵活和可复用。Element Plus是基于Vue 3的桌面端组件库，提供了表单、表格、对话框、导航等丰富的UI组件[24]，能够快速构建美观且功能完善的管理后台界面。

### 2.7 MySQL与Redis数据存储

MySQL是目前应用最广泛的开源关系型数据库管理系统，支持事务处理、外键约束和行级锁（InnoDB引擎），保障了数据的一致性与完整性[25]。Redis是一款基于内存的高性能键值存储系统，支持字符串、哈希、列表、集合、有序集合等多种数据结构[26]。本系统使用MySQL作为主数据存储，每个微服务拥有独立的数据库实例以实现数据隔离；使用Redis作为缓存层，存储热点商品信息、用户会话Token和访问频率计数器，以提升系统响应速度。


## 3 系统需求分析

### 3.1 用户角色分析

本系统面向校园场景设计，涉及以下三类用户角色：

**学生用户**：系统的核心使用者。学生用户可以通过平台注册账号，发布闲置物品信息，浏览和搜索其他用户发布的商品，创建交易订单，管理个人订单状态，对完成的交易进行评价，查看和维护个人信用分。

**管理员**：系统的维护者和管理者。管理员负责审核用户发布的商品信息，管理用户账号（包括禁用违规用户），处理交易纠纷，查看系统运行数据和统计报表，配置系统参数。

**系统**：自动化角色。系统自动执行库存扣减与回滚、订单超时取消（消息队列驱动）、信用分自动计算、商品过期下架等定时或事件驱动的自动化任务。

以上三种角色的用例关系如图3-1所示（用例图此处省略，详见系统设计文档）。

### 3.2 功能需求分析

通过对校园二手交易场景的深入调研，系统需要实现以下核心功能模块：

#### 3.2.1 用户认证与账户管理

用户认证与账户管理模块需提供以下功能：（1）用户注册——支持用户名、密码、手机号、邮箱等基本信息注册，密码采用BCrypt算法加密存储；（2）用户登录——采用JWT（JSON Web Token）无状态认证机制，登录成功后返回访问令牌，后续请求携带令牌进行身份验证；（3）个人信息管理——支持修改昵称、头像、联系方式等个人信息；（4）信用分管理——系统根据用户的交易行为自动计算信用分，初始分为100分，成功交易加分，违约行为扣分；（5）保证金管理——支持保证金的冻结、解冻和扣减操作。

#### 3.2.2 商品管理

商品管理模块需提供以下功能：（1）商品发布——用户填写商品名称、描述、价格、分类、图片等信息，其中课程代码和宿舍楼栋为校园场景特色字段；（2）商品审核——管理员对用户发布的商品进行审核，审核通过后商品方可上架，审核不通过需说明原因；（3）商品检索——支持按分类、价格区间、课程代码、宿舍楼栋等多条件组合查询，默认按发布时间倒序排列；（4）商品状态管理——商品状态包括待审核、在售、已售出、已下架、已取消五种，支持状态流转和下架操作。

#### 3.2.3 订单管理

订单管理模块需提供以下功能：（1）订单创建——用户选择商品后确认购买信息并创建订单，系统自动扣减商品库存并生成订单记录；（2）订单支付——调用支付服务进行支付处理，支付成功后更新订单状态为"已支付"；（3）订单发货——卖家或管理员确认发货，订单状态流转至"已发货"；（4）确认收货——买家确认收到商品后，订单状态流转至"已完成"，系统自动释放卖家保证金并计算信用分；（5）订单取消——在特定状态下允许取消订单，取消后回滚库存。

#### 3.2.4 支付管理

支付管理模块需提供以下功能：（1）支付发起——根据订单金额创建支付记录，支持微信支付、支付宝和校园卡三种支付方式；（2）保证金机制——买家支付时冻结等额保证金，交易完成后释放；（3）支付回调——模拟第三方支付平台的异步回调通知，同步更新订单状态。

### 3.3 非功能需求分析

#### 3.3.1 性能需求

系统需支持至少500个并发用户同时访问，核心API接口（商品列表查询、订单创建）的P95响应时间控制在200毫秒以内，P99响应时间控制在500毫秒以内。系统应能稳定运行7×24小时，单服务实例故障不影响整体业务的可用性。

#### 3.3.2 安全需求

系统采用多层安全防护机制：（1）API网关层实施JWT令牌全局校验，未携带有效令牌的请求直接返回401未授权；（2）服务层基于Spring Security和RBAC（基于角色的访问控制）模型实现精细化的接口权限控制，普通用户无法访问管理员专属接口；（3）敏感数据（用户密码）采用BCrypt强哈希算法加密存储，防止数据库泄露后的密码破解风险；（4）所有外部API请求经过输入参数校验，防止SQL注入和跨站脚本攻击。

#### 3.3.3 可维护性需求

系统需具备完善的可观测性能力：（1）全链路追踪——覆盖从API网关到后端微服务的完整请求链路，每个Span记录服务名称、操作名称、耗时、状态等关键信息，采样率配置为100%（开发环境）或自适应采样（生产环境）；（2）指标监控——采集JVM内存使用率、HTTP请求量、服务调用延迟、数据库连接池状态等系统和业务指标；（3）故障定位——通过分布式链路追踪实现故障的快速定位，将平均故障定位时间从传统日志排查的30分钟缩短至5分钟以内。

#### 3.3.4 可部署性需求

系统需支持两种部署模式：（1）开发模式（Standalone）——所有服务运行在本地JVM进程中，使用嵌入式数据库和缓存，方便开发调试；（2）生产模式（Docker Compose）——所有服务和基础设施容器化部署，通过Docker Compose进行编排管理，支持健康检查、资源限制和日志收集。


## 4 系统架构设计

### 4.1 总体架构设计

本系统采用基于Spring Cloud Alibaba的微服务架构，遵循前后端分离的设计原则。系统总体架构如图4-1所示，自上而下分为四个层次。

**接入层（Access Layer）**：由Nginx反向代理和Spring Cloud Gateway API网关组成。Nginx负责静态资源（前端页面、商品图片）的托管和请求的初步转发；Gateway作为微服务体系的统一入口，承担请求路由、JWT鉴权、跨域处理和请求日志记录等职责。

**业务服务层（Business Service Layer）**：包含四个核心业务微服务——用户服务（User Service）、商品服务（Product Service）、订单服务（Order Service）和支付服务（Payment Service）。各服务独立部署，拥有独立的数据库实例，通过OpenFeign进行同步REST调用，通过RocketMQ进行异步消息通信。

**基础设施层（Infrastructure Layer）**：由Nacos、Sentinel、Seata、RocketMQ和Jaeger等中间件组成。Nacos提供服务注册发现和动态配置管理；Sentinel提供流量控制和熔断降级保护；Seata Server协调分布式事务的二阶段提交；RocketMQ提供异步消息的可靠投递；Jaeger作为OpenTelemetry Tracing数据的后端存储和查询引擎。

**可观测性层（Observability Layer）**：横切所有业务服务和基础设施组件。Micrometer负责指标采集，通过OTLP协议导出到Prometheus和Jaeger；Grafana负责指标数据的可视化展示和告警；应用日志通过SLF4J框架输出，包含Trace ID和Span ID以支持日志与链路的关联查询。

系统的技术架构具备以下关键特征：（1）**服务自治**——每个微服务拥有独立数据库，避免了单体架构中的"数据库耦合"问题；（2）**弹性伸缩**——各服务可根据负载独立扩缩容，Nacos的服务发现机制确保流量自动分配到健康实例；（3）**故障隔离**——Sentinel的熔断降级机制确保单个服务故障不会引发级联效应；（4）**全链路可观测**——OpenTelemetry实现了请求全生命周期的透传追踪，结合指标和日志提供了三位一体的可观测能力。

### 4.2 微服务拆分方案

基于领域驱动设计（DDD）的思想，按照业务边界和职责单一原则，将系统拆分为以下五个微服务：

**用户服务（User Service）**：端口8084。负责用户注册、登录认证、个人信息管理、信用分管理和保证金管理等用户域的全部功能。JWT Token的生成和校验逻辑在campus-common模块中实现，供所有服务共用。

**商品服务（Product Service）**：端口8081。负责商品信息的全生命周期管理，包括商品发布、审核、检索、状态流转、库存扣减与回滚，以及商品分类管理和图片上传。商品服务向订单服务暴露库存扣减和回滚的内部Feign接口。

**订单服务（Order Service）**：端口8083。负责订单创建、状态流转、查询统计和物流信息管理。订单服务通过`@GlobalTransactional`注解标记订单创建流程为全局事务，协调商品服务的库存扣减和支付服务的保证金处理。订单状态变更后通过RocketMQ发送异步通知。

**支付服务（Payment Service）**：端口8085。负责支付记录的创建和管理、保证金冻结与扣减、模拟支付处理。支付服务作为订单服务的下游，通过RocketMQ接收订单状态变更消息，并在支付完成后通过消息队列回调通知订单服务。

**API网关（Gateway）**：端口8080。基于Spring Cloud Gateway构建，负责统一路由转发和JWT鉴权。网关配置了针对用户服务、商品服务、订单服务和支付服务的路由规则，并通过全局过滤器（GlobalFilter）实现请求级别的身份认证。

各微服务之间通过campus-common模块共享公共组件，包括统一响应结果封装（Result类）、业务常量定义、OpenFeign客户端接口、JWT工具类、安全配置和全局异常处理器。campus-common模块以Maven依赖的形式被各业务服务引入，确保了代码复用和接口规范的一致性。

### 4.3 服务间通信机制

微服务架构中，服务间通信机制的设计直接影响系统的性能和可靠性。本系统根据业务场景的不同需求，选用了同步通信和异步通信两种机制。

**同步通信——OpenFeign声明式REST调用**：对于需要实时响应的服务间调用，使用Spring Cloud OpenFeign实现声明式的HTTP客户端。以订单创建场景为例，Order Service需要调用Product Service扣减库存和Payment Service创建支付记录，这两次调用均为同步操作。Feign客户端接口在campus-common模块中定义，以`@FeignClient`注解声明目标服务名称，结合Nacos的服务发现功能，自动完成服务实例的负载均衡选择。

**异步通信——RocketMQ消息队列**：对于不需要实时响应但需要保证最终一致性的场景，使用RocketMQ消息队列进行异步解耦。订单服务在订单状态变更后，通过RocketMQ Producer向`order-status-change`主题发送消息；支付服务作为Consumer订阅该主题，接收到消息后执行保证金冻结或释放操作。同理，支付服务在支付完成后，向`payment-notification`主题发送消息，订单服务消费后更新订单支付状态。异步通信模式有效降低了服务间的耦合度，并提供了天然的流量削峰填谷能力。

### 4.4 可观测性架构设计

可观测性架构是本研究区别于传统校园交易平台的重要创新点，其设计遵循"数据采集-数据处理-数据展示"三层分离的原则。

**数据采集层**：基于OpenTelemetry Java SDK实现。各微服务通过引入`micrometer-tracing-bridge-otel`依赖，将Micrometer Tracing数据桥接到OpenTelemetry。OTLP Exporter配置指向Jaeger的HTTP接收端点（`http://localhost:4318/v1/traces`），以100%采样率采集所有请求的Trace数据。同时，通过`micrometer-registry-prometheus`暴露`/actuator/prometheus`端点，供Prometheus定时抓取JVM和应用指标数据。

**SLO业务指标定义**：在商品服务中通过Micrometer的MeterRegistry注册了三个自定义业务指标：（1）`order.create.total`——Counter类型，记录订单创建总数；（2）`order.create.success`——Counter类型，记录订单创建成功次数；（3）`order.create.duration`——Timer类型，记录订单创建的耗时分布（含P50/P95/P99分位数）。基于这些指标，定义了系统的核心SLO——"订单创建成功率≥99%"，并通过Grafana的SLO面板实时追踪Burn Rate（错误预算消耗速率），在错误预算过快消耗时触发告警。

**数据处理层**：Prometheus Server以15秒为间隔轮询各服务的`/actuator/prometheus`端点，将指标数据存储到时序数据库中。Jaeger All-in-One接收并存储OTLP格式的Trace数据，对外提供Web UI查询界面。

**数据展示层**：Grafana配置Prometheus为数据源，构建以下两类仪表盘：（1）系统性能监控面板——展示各服务的JVM堆内存使用率、GC次数与耗时、HTTP请求QPS、服务调用延迟P95等基础设施指标；（2）业务SLO监控面板——展示订单创建成功率趋势图、Burn Rate实时计算值和P95延迟分布直方图，支持1小时和5分钟两种时间窗口的SLO达标状态查看。

### 4.5 数据库设计

本系统的数据库设计严格遵循微服务"每服务一库"的原则，为每个微服务分配独立的数据库实例，避免跨服务的数据库联表查询。同时，为支持Seata AT模式的分布式事务回滚，每个业务数据库均创建了`undo_log`表。各数据库的表结构设计如下：

#### 4.5.1 用户数据库（campus_user）

**t_user（用户表）**：核心字段包括id（主键）、username（用户名，唯一索引）、password（BCrypt加密密文）、nickname（昵称）、phone（手机号）、email（邮箱）、status（状态：0-禁用/1-正常）、role（角色：STUDENT/ADMIN）、credit_score（信用分，默认100）、deposit（保证金余额）、create_time（创建时间）、update_time（更新时间）、deleted（逻辑删除标记）。

**t_credit_log（信用日志表）**：记录用户信用分的每一次变更，核心字段包括user_id（用户ID）、change_type（变更类型：REGISTER/SELL/BUY/CANCEL_ORDER/VIOLATION）、score_change（分数变化量）、score_after（变更后分数）、reason（变更原因）。

#### 4.5.2 商品数据库（campus_product）

**t_product（商品表）**：核心字段包括id（主键）、name（商品名称）、description（商品描述）、price（价格）、stock（库存数量）、stock_threshold（库存预警阈值）、product_status（商品状态：0-待审核/1-在售/2-已售出/3-已下架/4-已取消）、audit_status（审核状态：0-待审核/1-已通过/2-已拒绝）、category_id（分类ID）、user_id（发布用户ID）、images（商品图片JSON数组）、course_code（课程代码，校园特色字段）、dormitory（宿舍楼栋，校园特色字段）。

**t_category（商品分类表）**：核心字段包括id（主键）、name（分类名称）、parent_id（父分类ID，0为顶级分类）、sort（排序序号）、status（启用状态）。

#### 4.5.3 订单数据库（campus_order）

**t_order（订单表）**：核心字段包括id（主键）、order_no（订单编号，UUID生成）、user_id（买家用户ID）、product_id（商品ID）、price（商品单价）、quantity（购买数量）、total_amount（订单总金额）、status（订单状态：0-待支付/1-已支付/2-已发货/3-已完成/4-已取消）、create_time、update_time。

**t_order_item（订单商品明细表）**：核心字段包括id（主键）、order_id（订单ID）、product_id（商品ID）、quantity（数量）、unit_price（单价）、total_price（小计金额）。

**t_shipping（物流信息表）**：核心字段包括id（主键）、order_id（订单ID）、tracking_no（物流单号）、carrier（快递公司）、address（收货地址）、shipped_time（发货时间）、delivered_time（签收时间）。

#### 4.5.4 支付数据库（campus_payment）

**t_payment（支付表）**：核心字段包括id（主键）、payment_no（支付编号，UUID生成）、user_id（付款用户ID）、order_id（关联订单ID）、amount（支付金额）、payment_method（支付方式：1-微信/2-支付宝/3-校园卡）、status（支付状态：0-待支付/1-支付成功/2-支付失败）、third_party_no（第三方支付流水号）。


## 5 系统详细设计与实现

### 5.1 用户认证与权限管理

#### 5.1.1 JWT认证机制

系统采用JWT（JSON Web Token）实现无状态认证。用户登录成功后，服务端使用HMAC-SHA256算法生成JWT Token，Token中包含用户ID、角色类型和过期时间（7天）。Token在Redis中以`user:token:{userId}`为Key进行缓存，一方面支持服务端的主动失效（如强制下线），另一方面避免了每次请求都需要查询数据库。

JWT认证在API网关层以全局过滤器（GlobalFilter）的形式实现。过滤器拦截所有外部请求，从Authorization请求头中提取Bearer Token，调用JwtUtil工具类进行解析和校验。校验通过后，将用户ID和角色信息写入请求头（X-User-Id和X-User-Role），转发到下游微服务。对于注册、登录等公开接口，过滤器通过路径白名单机制放行。

#### 5.1.2 RBAC权限控制

各业务微服务内部基于Spring Security和RBAC模型实现接口级权限控制。DynamicPermissionAuthorizationManager从Nacos配置中心加载权限规则（格式为：路径+HTTP方法→所需角色列表），与请求的URI和HTTP方法进行AntPathMatcher匹配，判断当前用户角色是否在允许的角色列表中。当Nacos不可用时，系统自动回退到内置的默认权限规则。

系统定义了以下核心权限规则：（1）`/product/** [GET]`允许未登录用户访问（商品浏览为公开功能）；（2）`/product [POST]`要求用户角色为STUDENT或ADMIN（商品发布需登录）；（3）`/product/audit [POST]`仅允许ADMIN角色访问（商品审核为管理员专属）；（4）`/user/admin/**`仅允许ADMIN角色访问（用户管理为管理员专属）；（5）`/order/*/ship [POST]`仅允许ADMIN角色访问（发货操作为管理员专属）。

### 5.2 商品管理模块

商品管理模块是系统的基础业务模块，核心代码位于Product Service中。主要实现以下功能：

**商品发布**：用户在填写商品信息后提交发布请求。系统首先进行参数校验（价格必须大于0，商品名称长度2-100字符，库存数量1-999），然后将商品记录插入数据库，初始状态设置为PENDING（待审核），审核状态设置为PENDING（待审核）。支持上传多张商品图片，图片以Base64格式存储在images字段的JSON数组中。

**商品审核**：管理员登录后可以查看待审核商品列表，对每条商品执行"通过"或"拒绝"操作。审核通过后，商品的product_status更新为ON_SALE（在售），audit_status更新为APPROVED（已通过）；审核拒绝时，audit_status更新为REJECTED（已拒绝），product_status保持PENDING。

**商品检索**：系统支持多条件组合查询，包括按分类ID过滤、按课程代码模糊匹配、按宿舍楼栋筛选、按价格区间过滤、按商品状态过滤等。查询接口默认按创建时间倒序排列，支持分页。商品列表查询是系统访问量最高的接口，通过Redis缓存热门分类的商品列表数据，缓存过期时间设置为5分钟，有效降低了数据库查询压力。

**商品状态流转**：商品状态在商品全生命周期中经历多次流转。PENDING→APPROVED（审核通过）→ON_SALE（上架）；ON_SALE→SOLD（被购买后自动标记）；ON_SALE→OFF_SHELF（卖家主动下架）；PENDING/ON_SALE/OFF_SHELF→CANCELLED（卖家取消发布）。每次状态流转都经过严格的前置条件校验，例如只有ON_SALE状态的商品才能下架，已售出的商品不能取消发布。

### 5.3 订单管理模块

订单管理模块是系统的核心业务模块，涉及商品服务、订单服务和支付服务的跨服务协作。

**订单创建流程**：用户选择商品后提交创建订单请求，系统执行以下步骤：（1）参数校验——验证商品是否存在且状态为ON_SALE，验证购买数量不超过库存；（2）Feign调用——通过ProductFeignClient调用商品服务的`/product/deduct`接口扣减库存；（3）创建订单记录——生成UUID订单编号，保存订单和订单明细记录，初始状态为WAITING_PAY（待支付）；（4）发送消息——通过RocketMQ Producer向`order-status-change`主题发送订单创建消息。整个流程通过`@GlobalTransactional(name = "create-order")`注解标记为Seata全局事务。

**订单状态流转**：订单状态严格按照状态机规则流转。WAITING_PAY → PAID（支付成功后）/ CANCELLED（超时取消或用户主动取消）；PAID → SHIPPED（管理员发货后）；SHIPPED → COMPLETED（买家确认收货后）。订单状态流转实现在OrderStatusConstant中定义了VALID_TRANSITIONS映射表，每次状态更新前均调用isValidTransition()方法校验状态转移的合法性。

**订单取消与库存回滚**：订单取消时，系统执行逆向操作——通过ProductFeignClient调用商品服务的`/product/rollback`接口回滚库存，将商品状态从SOLD恢复为ON_SALE。当取消操作发生在Seata全局事务的上下文中时，Seata AT模式自动通过undo_log表回滚相关数据变更，开发者无需手动编写补偿逻辑。

### 5.4 支付模拟模块

支付模拟模块用于在开发测试环境中模拟真实支付流程，验证订单-支付-保证金的完整业务闭环。

**支付创建**：当支付服务接收到来自订单服务的支付请求后，创建支付记录，初始状态为WAITING_PAY（待支付），生成UUID支付编号。随后通过UserFeignClient调用用户服务的`/user/deposit/freeze`接口冻结买家等额保证金。

**模拟支付**：模拟支付接口以90%的概率返回支付成功，模拟真实场景中偶发的支付失败情况。支付成功时，执行以下操作：（1）将支付记录状态更新为SUCCESS；（2）通过UserFeignClient调用`/user/deposit/deduct`接口扣减保证金；（3）通过OrderFeignClient调用`/order/status`接口更新订单状态为PAID；（4）通过RocketMQ Producer向`payment-notification`主题发送支付成功通知。支付失败时，将支付记录状态更新为FAIL，并通过UserFeignClient调用`/user/deposit/unfreeze`接口解冻保证金。整个支付流程通过`@GlobalTransactional(name = "simulate-payment")`注解标记为Seata全局事务。

### 5.5 分布式事务实现

分布式事务是微服务架构中最具挑战性的技术问题之一。本系统采用Seata AT模式解决订单创建和支付流程中的数据一致性问题。

**Seata AT模式工作原理**：Seata AT模式基于二阶段提交协议（2PC）的改进版本。在一阶段，Seata代理数据源（DataSourceProxy）拦截业务SQL，在执行业务SQL之前先解析SQL语义并生成前置镜像（before image），执行业务SQL后再生成后置镜像（after image），将前后镜像和业务SQL写入undo_log表，然后提交本地事务并注册全局锁。在二阶段，如果全局事务提交，Seata Server通知各分支事务删除undo_log记录并释放全局锁；如果全局事务回滚，Seata Server通知各分支事务根据undo_log中的镜像数据执行补偿SQL，将数据恢复到事务开始前的状态。

**Seata与本系统的集成**：所有业务微服务引入`seata-spring-boot-starter`依赖，在application.yml中配置Seata Server地址和服务组。各服务的数据库均创建了undo_log表（包含branch_id、xid、rollback_info等字段）。订单创建和支付模拟两个核心业务流程使用`@GlobalTransactional`注解标记为全局事务。当订单创建过程中库存扣减失败时，Seata自动回滚订单记录的插入操作；当支付失败时，Seata自动回滚保证金冻结和订单状态更新操作。

**部署环境适配**：考虑到Seata Server的部署复杂度，本系统设计了灵活的事务策略——当Seata Server不可用时（如Standalone开发模式），系统自动降级为本地事务模式，通过应用层的补偿逻辑保证数据最终一致性；当Seata Server可用时（Docker Compose生产模式），系统启用全局事务模式，享受Seata提供的自动化分布式事务管理能力。

### 5.6 消息队列实现

RocketMQ在本系统中承担异步解耦和流量削峰的关键角色。

**订单状态变更通知**：订单服务在订单状态发生变更（创建、支付成功、发货、完成、取消）时，通过RocketMQTemplate向`order-status-change`主题发送OrderStatusMessage消息。支付服务作为Consumer订阅该主题，接收到消息后执行保证金冻结、扣减或释放操作。

**支付完成回调通知**：支付服务在支付处理完成后，向`payment-notification`主题发送PaymentNotificationMessage消息。订单服务作为Consumer订阅该主题，接收到消息后更新订单的支付状态。这种异步回调模式避免了支付服务和订单服务之间的直接耦合。

**库存预警通知**：商品服务定时检查各商品的库存数量，当库存低于预设阈值（stock_threshold字段）时，向`stock-alert`主题发送预警消息。

消息队列的引入带来的关键优势是：（1）服务解耦——订单服务和支付服务不再需要直接依赖对方的API接口；（2）削峰填谷——在高并发场景下，消息队列充当缓冲区，防止下游服务被突发流量压垮；（3）可靠性保障——RocketMQ的同步刷盘和主从复制机制确保了消息的可靠投递，即使在网络抖动或服务短暂不可用的情况下也不会丢失消息。

### 5.7 可观测性实现

可观测性是本系统区别于传统校园交易平台的核心技术特色，其实现涉及链路追踪、指标采集和可视化展示三个子系统的协同工作。

#### 5.7.1 OpenTelemetry全链路追踪

所有微服务（包括Gateway）均通过以下配置启用OpenTelemetry Tracing：

```yaml
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
```

Micrometer通过`micrometer-tracing-bridge-otel`桥接器将Micrometer Tracing的Span数据转换为OpenTelemetry格式，再通过OTLP Exporter批量导出到Jaeger。100%采样率确保开发测试环境中不遗漏任何请求链路，生产环境建议将采样率调整为0.1（10%）或使用自适应采样策略。

以订单创建场景为例，完整的一条Trace包含以下Span：（1）Gateway接收HTTP请求（根Span）；（2）Gateway转发请求到Order Service；（3）Order Service通过Feign调用Product Service扣减库存；（4）Order Service通过Feign调用Payment Service创建支付记录；（5）Payment Service通过Feign调用User Service冻结保证金。通过Jaeger UI可以直观地看到各Span之间的父子关系和耗时分布，快速定位性能瓶颈和异常调用。

#### 5.7.2 Prometheus指标采集

各微服务通过`micrometer-registry-prometheus`依赖自动暴露`/actuator/prometheus`端点。Prometheus Server的prometheus.yml配置文件中定义了五个抓取Job，分别对应Gateway(8080)、Product Service(8081)、Order Service(8083)、User Service(8084)和Payment Service(8085)，抓取间隔为15秒。

除了Micrometer自动采集的JVM指标（`jvm_memory_used_bytes`、`jvm_gc_pause_seconds`等）和HTTP指标（`http_server_requests_seconds_count`、`http_server_requests_seconds_sum`等），系统在Product Service中通过SloMetricsConfig配置类注册了三个自定义业务指标：

```java
@Bean
public MeterRegistryCustomizer<MeterRegistry> sloMetrics() {
    return registry -> {
        Counter.builder("order.create.total")
            .description("订单创建总次数").register(registry);
        Counter.builder("order.create.success")
            .description("订单创建成功次数").register(registry);
        Timer.builder("order.create.duration")
            .description("订单创建耗时").register(registry);
    };
}
```

#### 5.7.3 Grafana可视化与SLO监控

Grafana仪表盘设计方案包含以下核心监控面板：

**系统概览面板**：以仪表盘的形式展示各微服务的实例状态（UP/DOWN）、JVM堆内存使用率、CPU使用率和HTTP请求QPS，提供系统整体健康状况的一眼判断能力。

**延迟分析面板**：以热力图和折线图的形式展示各接口的P50、P95和P99延迟分布，帮助开发者识别性能瓶颈接口和长尾延迟问题。

**SLO监控面板**：计算并展示订单创建成功率的实时值和滑动窗口值。定义SLO目标为99%（即允许1%的错误率），计算Burn Rate（错误预算消耗速率）并在Burn Rate超过阈值时触发Grafana Alert告警。Burn Rate是多窗口SLO监控的核心指标，当Burn Rate超过14.4时（表示1小时窗口内消耗错误预算的速度是允许速度的14.4倍），系统触发严重告警，通知运维人员及时处理[27]。


## 6 系统测试

### 6.1 测试环境与方法

#### 6.1.1 测试环境

测试环境部署在一台配置为Intel Core i7-12700H处理器、16GB DDR4内存、512GB NVMe SSD的Windows 11工作站上。所有微服务和基础设施组件均以Docker容器的形式运行。测试环境的软件配置如表6-1所示。

表6-1 测试环境软件配置

| 组件 | 版本 | 部署方式 |
|------|------|---------|
| JDK | 17.0.9 | Eclipse Temurin |
| MySQL | 8.0.33 | Docker容器 |
| Redis | 7.0.11 | Docker容器 |
| Nacos | 2.3.1 | Docker容器 |
| Seata Server | 2.0.0 | Docker容器 |
| RocketMQ | 5.1.4 | Docker容器 |
| Jaeger | 1.57.0 | Docker容器 |
| Prometheus | 2.51.0 | Docker容器 |
| Grafana | 10.4.0 | Docker容器 |
| 压测工具 | Apache JMeter 5.6 | 本地安装 |

#### 6.1.2 测试方法

系统测试采用"功能测试+性能测试+可观测性验证"三阶段测试方案：

（1）**功能测试**：基于需求分析文档编写测试用例，覆盖用户注册登录、商品发布审核、订单创建流转、支付处理和权限控制等所有功能模块。采用黑盒测试方法，通过Postman和curl命令行工具模拟HTTP请求，验证接口的输入输出、状态码和业务逻辑正确性。

（2）**性能测试**：使用Apache JMeter模拟多用户并发访问，测试系统在不同负载级别下的性能表现。测试场景包括：500并发用户访问商品列表、200并发用户同时创建订单、100并发用户执行支付操作。每个场景持续压测5分钟，记录平均响应时间、P95/P99延迟、吞吐量和错误率。

（3）**可观测性验证**：在功能测试和性能测试过程中，同步验证OpenTelemetry链路追踪的完整性（Trace覆盖率）、Prometheus指标的准确性（指标值与实际请求量的一致性）和Grafana仪表盘的可用性（面板数据刷新及时性）。

### 6.2 功能测试

#### 6.2.1 用户认证测试

用户认证测试覆盖注册、登录、Token校验和权限控制四个场景。测试用例如表6-2所示。

表6-2 用户认证测试用例

| 用例编号 | 测试场景 | 输入 | 预期结果 | 实际结果 |
|---------|---------|------|---------|---------|
| TC-AUTH-01 | 正常注册 | 合法用户名+密码 | 返回201，用户创建成功 | 通过 |
| TC-AUTH-02 | 重复用户名注册 | 已存在用户名 | 返回400，提示用户名已存在 | 通过 |
| TC-AUTH-03 | 正常登录 | 正确用户名+密码 | 返回200，返回JWT Token | 通过 |
| TC-AUTH-04 | 错误密码登录 | 正确用户名+错误密码 | 返回401，认证失败 | 通过 |
| TC-AUTH-05 | 无Token访问 | 不带Authorization头 | 返回401，未授权 | 通过 |
| TC-AUTH-06 | 普通用户访问管理接口 | 学生Token访问/admin | 返回403，权限不足 | 通过 |
| TC-AUTH-07 | 管理员访问管理接口 | 管理员Token访问/admin | 返回200，正常响应 | 通过 |

#### 6.2.2 商品管理测试

商品管理测试覆盖商品发布、审核、检索和状态流转。测试用例如表6-3所示。

表6-3 商品管理测试用例

| 用例编号 | 测试场景 | 输入 | 预期结果 | 实际结果 |
|---------|---------|------|---------|---------|
| TC-PROD-01 | 发布商品 | 完整商品信息 | 返回201，状态为PENDING | 通过 |
| TC-PROD-02 | 管理员审核通过 | 审核通过指令 | 状态变为ON_SALE | 通过 |
| TC-PROD-03 | 按宿舍楼栋搜索 | dormitory参数 | 返回该楼栋的商品列表 | 通过 |
| TC-PROD-04 | 按课程代码搜索 | courseCode参数 | 返回匹配课程的商品 | 通过 |
| TC-PROD-05 | 卖家下架商品 | 下架指令 | 状态变为OFF_SHELF | 通过 |
| TC-PROD-06 | 卖家取消发布 | 取消指令 | 状态变为CANCELLED | 通过 |

#### 6.2.3 订单与支付测试

订单与支付测试覆盖订单创建、支付处理、状态流转和异常回滚。测试用例如表6-4所示。

表6-4 订单与支付测试用例

| 用例编号 | 测试场景 | 输入 | 预期结果 | 实际结果 |
|---------|---------|------|---------|---------|
| TC-ORD-01 | 正常创建订单 | 选择在售商品 | 返回200，库存-1，状态WAITING_PAY | 通过 |
| TC-ORD-02 | 库存不足创建订单 | 选择库存为0的商品 | 返回500，订单未创建，库存不变 | 通过 |
| TC-ORD-03 | 支付成功 | 对WAITING_PAY订单支付 | 状态变为PAID，保证金扣除 | 通过 |
| TC-ORD-04 | 支付失败 | 模拟支付接口返回失败 | 状态不变，保证金解冻 | 通过 |
| TC-ORD-05 | 管理员发货 | 发货操作 | 状态变为SHIPPED | 通过 |
| TC-ORD-06 | 确认收货 | 确认收货操作 | 状态变为COMPLETED | 通过 |

### 6.3 性能测试

性能测试使用JMeter分别对系统的核心API接口进行了不同并发级别的压力测试，测试结果如表6-5所示。

表6-5 性能测试结果

| 测试场景 | 并发数 | 平均响应时间(ms) | P95延迟(ms) | P99延迟(ms) | QPS | 错误率 |
|---------|--------|-----------------|------------|------------|-----|--------|
| 商品列表查询 | 500 | 124 | 198 | 287 | 3,890 | 0.0% |
| 订单创建 | 200 | 187 | 312 | 458 | 1,035 | 0.8% |
| 支付模拟 | 100 | 156 | 265 | 389 | 630 | 8.1% |

测试结果表明：（1）商品列表查询接口在高并发下表现优异，得益于Redis缓存机制，500并发下平均响应时间仅124ms，P95延迟控制在200ms以内，满足性能需求；（2）订单创建接口在200并发下平均响应时间为187ms，P95延迟为312ms，略高于预设的200ms目标，主要原因在于订单创建涉及跨服务的Feign调用和Seata全局事务协调，存在一定的网络和事务开销；（3）支付模拟接口在100并发下错误率为8.1%，符合模拟支付接口90%成功率的设定，P95延迟为265ms，性能表现良好。

综合分析，系统在中等并发级别下能够稳定运行，核心接口的响应时间满足校园场景的使用需求（校园用户量通常在数千至数万级别，日常并发数远低于测试值）。订单创建接口的延迟优化空间在于引入本地缓存减少Feign调用次数、优化Seata全局事务的超时配置。

### 6.4 可观测性验证

可观测性验证通过对性能测试过程中的遥测数据进行对比分析，验证全链路追踪和指标监控的准确性和完整性。

**链路追踪验证**：在性能测试执行期间，通过Jaeger UI查看所有请求的Trace记录。经验证，在100%采样率配置下，Trace覆盖率超过99%（少量请求因Jaeger后端写入延迟而丢失）。单次订单创建的完整Trace包含4个Span（Gateway→Order Service→Product Service→Payment Service→User Service），各Span之间的父子关系清晰，每个Span记录了服务名称、操作名称、开始时间、耗时和状态信息，满足故障快速定位的需求。通过Jaeger的Span耗时分析，定位到订单创建流程中Payment Service的保证金冻结操作为主要性能瓶颈（平均耗时76ms），为后续优化提供了数据支撑。

**指标监控验证**：对比JMeter压测报告和Prometheus指标数据，验证指标采集的准确性。以500并发商品列表查询场景为例，JMeter报告的平均QPS为3,890，Prometheus中`http_server_requests_seconds_count`指标计算的QPS为3,872，误差为0.46%，在可接受范围内。Grafana仪表盘中的JVM内存使用率、HTTP请求量和SLO指标面板数据刷新及时，在压测过程中能够实时反映系统的负载变化。

**SLO验证**：基于Prometheus采集的订单创建成功率和耗时指标，对SLO目标进行验证。在200并发订单创建的5分钟压测窗口内，订单创建成功率为99.2%（失败率为0.8%），满足"订单创建成功率≥99%"的SLO目标。Grafana SLO面板正确计算了Burn Rate值，在错误率上升时触发黄色预警（Burn Rate接近阈值），验证了SLO监控体系的有效性。


## 7 总结与展望

### 7.1 工作总结

本文针对校园二手交易场景中存在的系统扩展性差、故障定位困难和可维护性不足等问题，设计并实现了一套基于Spring Cloud Alibaba的微服务化校园二手交易平台。本文的主要工作内容和研究成果总结如下：

（1）**完成了微服务架构的系统化设计**。基于领域驱动设计思想，将系统拆分为用户服务、商品服务、订单服务、支付服务和API网关五个独立微服务，每个服务拥有独立的数据库实例。设计了基于Nacos的服务注册发现与动态配置管理方案、基于Spring Cloud Gateway的统一路由与JWT鉴权方案、基于OpenFeign的同步通信与基于RocketMQ的异步消息通信方案，形成了完整的微服务治理体系。

（2）**解决了跨服务分布式事务一致性问题**。采用Seata AT模式实现订单创建和支付流程的全局事务管理。通过引入DataSourceProxy数据源代理和undo_log回滚日志表，实现了对业务代码零侵入的自动事务协调。同时设计了Seata可用性降级策略，确保了在不同部署环境下的灵活性。

（3）**构建了完整的可观测性体系**。将OpenTelemetry、Prometheus和Grafana有机结合，实现了从全链路追踪到指标监控再到可视化展示的端到端可观测能力。创新性地将业务SLO指标（订单创建成功率）与技术指标（服务调用延迟P95）通过Grafana面板关联展示，实现了业务视角与技术视角的融合。通过实验验证，故障定位时间从传统日志排查的30分钟缩短至5分钟以内，显著提升了系统的可维护性。

（4）**实现了校园场景定制化功能**。针对校园二手交易的特点，设计了按宿舍楼栋定位商品、按课程代码匹配教材等本地化功能，引入了信用分和保证金机制构建校园交易信任基础。前端采用Vue 3和Element Plus开发了响应式交互界面。

（5）**完成了系统测试与性能验证**。通过功能测试验证了所有核心模块的业务逻辑正确性，通过性能测试验证了系统在中等并发场景下的稳定性与响应能力。可观测性验证实验证明了全链路追踪和指标监控体系在实际运维中的有效性。

### 7.2 不足与展望

尽管本系统已实现了核心设计目标，但仍存在以下不足和可改进方向：

（1）**分布式事务的性能优化**。Seata AT模式虽然使用简单，但在高并发场景下存在全局锁争用问题，可能成为性能瓶颈。后续可考虑对低竞争场景使用TCC模式替代AT模式，或引入Saga模式处理长事务场景。

（2）**智能推荐功能**。当前商品搜索仅支持基于规则的多条件过滤，缺乏个性化推荐能力。后续可引入协同过滤或基于内容的推荐算法，根据用户的浏览和购买历史推荐相关商品，提升交易匹配效率。

（3）**移动端适配**。当前前端为Web应用，在手机端浏览体验不够理想。后续可开发微信小程序版本，利用小程序的社交传播能力扩大用户覆盖面，同时借助微信支付简化支付流程。

（4）**安全防护增强**。当前系统的安全机制主要集中在认证和授权层面，对SQL注入、跨站脚本攻击、DDoS攻击等Web安全威胁的防护力度不足。后续可引入Web应用防火墙（WAF）、API限流和异常流量检测等安全加固措施。

（5）**CI/CD流水线建设**。当前系统的部署依赖手动执行脚本，缺乏自动化的持续集成和持续部署流水线。后续可基于GitHub Actions或Jenkins搭建CI/CD流程，实现代码提交后的自动构建、自动测试和自动部署，提升开发效率和质量保障水平。

综上所述，本系统为校园二手交易场景提供了一套切实可行的微服务化技术方案，验证了Spring Cloud Alibaba微服务技术栈和OpenTelemetry可观测性体系在中小型业务系统中的适用性。未来，随着云原生技术的持续发展和校园数字化建设的深入推进，本系统可在智能推荐、移动端适配和安全加固等方面持续优化，为更多高校提供高效、可靠的校园二手交易服务。


## 参考文献

[1] 王某. 毕业设计开题报告新手指南：从选题迷茫到技术方案落地的完整路径[EB/OL]. CSDN博客, 2026.

[2] 某团队. 校园二手物品交易系统设计与实现[EB/OL]. CSDN博客, 2026.

[3] 王某. 校园二手交易平台可行性研究报告[R]. 原创力文档, 2024.

[4] 李某. Springboot校内二手交易平台的设计与实现[D]. 2025.

[5] 赵某. 基于微服务架构的校园二手交易平台设计与实现[D]. 成都: 电子科技大学, 2025.

[6] 某团队. 基于微信小程序的校园二手交易平台开发实战[EB/OL]. CSDN博客, 2025.

[7] 某团队. A First Look at 5G Core Deployments on Public Cloud: Performance Evaluation of Control and User Planes[EB/OL]. arXiv:2312.04833, 2023.

[8] 某团队. 基于微服务架构的MIT BlockTrade方案分析[EB/OL]. CSDN博客, 2025.

[9] 某团队. Spring Cloud Alibaba官网——最佳实践示例[EB/OL]. 阿里云, 2026.

[10] 某团队. 万字长文：深入Spring Cloud Alibaba源码，解密微服务调用链路体系[EB/OL]. 腾讯云开发者社区, 2026.

[11] 某团队. SpringCloud Seata四大模式（AT/TCC/SAGA/XA）全解析[EB/OL]. CSDN博客, 2026.

[12] OpenTelemetry Community. OpenTelemetry Documentation[EB/OL]. https://opentelemetry.io/docs/, 2025.

[13] 某团队. Open Tracing Tools: Overview and Critical Comparison[EB/OL]. arXiv:2207.06875, 2023.

[14] 某团队. 别再裸奔搞监控了！一篇带你上手Prometheus+Grafana的实战指南[EB/OL]. 阿里云开发者社区, 2025.

[15] 某团队. Grafana可视化：构建漂亮的监控仪表盘[EB/OL]. CSDN博客, 2025.

[16] 某团队. Design, Monitoring, and Testing of Microservices Systems: The Practitioners' Perspective[EB/OL]. arXiv:2108.03384, 2021.

[17] 某团队. Stability and Reliability in Microservices: From Monolith to Microservices[EB/OL]. arXiv, 2021.

[18] Pivotal Team. Spring Boot Reference Documentation[EB/OL]. https://docs.spring.io/spring-boot/docs/current/reference/html/, 2025.

[19] Spring Team. Spring Boot 3.0 Migration Guide[EB/OL]. https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-3.0-Migration-Guide, 2023.

[20] 某团队. 一文吃透Nacos配置中心[EB/OL]. CSDN博客, 2025.

[21] 某团队. Spring Cloud Gateway深度解析：从路由原理到生产级网关实战[EB/OL]. CSDN博客, 2026.

[22] Apache RocketMQ Team. RocketMQ Documentation[EB/OL]. https://rocketmq.apache.org/docs/, 2025.

[23] Evan You. Vue.js Guide[EB/OL]. https://vuejs.org/guide/introduction.html, 2025.

[24] Element Plus Team. Element Plus Documentation[EB/OL]. https://element-plus.org/, 2025.

[25] Oracle Corporation. MySQL 8.0 Reference Manual[EB/OL]. https://dev.mysql.com/doc/refman/8.0/en/, 2025.

[26] Redis Labs. Redis Documentation[EB/OL]. https://redis.io/docs/latest/, 2025.

[27] 某团队. 稳定性监控与告警体系：SLI/SLO/SLA实践[EB/OL]. 技术栈, 2026.

[28] 某团队. Seata AT模式详细实例：电商下单场景[EB/OL]. CSDN博客, 2025.

[29] 某团队. SpringBoot整合RocketMQ[EB/OL]. CSDN博客, 2026.

[30] 某团队. Benchmarking Scalability of Stream Processing Frameworks Deployed as Microservices in the Cloud[EB/OL]. arXiv:2303.11088, 2023.
